import React from "react";
//import { recipeData } from "./test.json"

/*
export const Recipes = () => {
    return (
      <>
        <div className="recipe-container">Recipe Output
         {recipeData.map((data, key) => {
          return (
            <div key={key}>
              {data.name +
                " , " +
                data.ingredients +
                " ," +
                data.steps +
                ", " +
                data.notes +
                "," +
                data.type}
        </div>
          );
        })}
        </div>
      </>
    );
  };*/